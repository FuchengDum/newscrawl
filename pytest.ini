[pytest]
markers =
    integration: marks tests that make real network requests (deselect with '-m "not integration"')

# Default: skip integration tests. Run with `pytest -m integration` to include them.
addopts = -m "not integration"
